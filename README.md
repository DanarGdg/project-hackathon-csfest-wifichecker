📄 Description
A application that checks if the user is connected to a specific WiFi network (with “pnj” in its SSID) and validates attendance for campus usage
🧰 How to run
Follow these steps to get the application running locally:


Install dependencies
npm install
# or
yarn install



Start the development server
node src/index.js


Open your browser
Navigate to http://localhost:3000.check-wifi to check wifi ssid 
